# 部署记录

更新时间：2026-04-18

## 当前可用地址

- Demo 访问地址：`http://rui.vin/mold-demo/`

## 当前部署方式

- 站点以静态导出方式部署
- 路径前缀为 `/mold-demo`
- 通过服务器现有 `Caddy` 网关接入
- 没有覆盖根域现有博客站点

## 备注

- 当前 `HTTPS` 未打通，因此暂时使用 `HTTP`
- 如果后续需要与主站完全隔离，可以改成独立端口或独立子域名
